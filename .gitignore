# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# local env/secrets
.env
.env.*
!.env.example

# logs and local coverage/cache artifacts
*.log
.coverage
.coverage.*
htmlcov/
.ruff_cache/
.mypy_cache/

# agent/assistant instruction files (kept out of the published repo)
AGENTS.md
CLAUDE.md
.claude/

# local-only scratch / planning notes (never committed)
internal/

# throwaway traces written by sdk/python/scripts/live_trace_demo.py — they
# land in web/public, get copied into web/out by the static export, and would
# otherwise be baked into the shipped _ui bundle by scripts/build_ui.py
web/public/live-demo__*.jsonl
